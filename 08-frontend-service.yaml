apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: smart-todo-app
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend
